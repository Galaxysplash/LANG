#pragma once

#include "data_types.h"

constexpr inline unsigned char TXT_INDICATOR = '"';

inline hash_table(double) num_list;
inline hash_table(std::string) txt_list;
inline hash_table(bool) bit_list;